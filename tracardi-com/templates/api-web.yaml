{{ if and .Values.secrets.dockerHub .Values.staging.license.existingSecret }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-web-deployment
  labels:
    {{- include "tracardi.labels" (dict "ctx" . "component" "api-web") | nindent 4 }}
spec:
  replicas: {{ .Values.production.replicas }}
  selector:
    matchLabels:
      {{- include "tracardi.selectorLabels" (dict "ctx" . "component" "api-web") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "tracardi.podLabels" (dict "ctx" . "component" "api-web") | nindent 8 }}
    spec:
      imagePullSecrets:
        - name: {{ .Values.secrets.dockerHub | quote }}
      containers:
        - name: api-web
          image: "{{ .Values.production.image.repository }}:{{ .Values.production.image.tag }}"
          imagePullPolicy: {{ .Values.production.image.pullPolicy }}
          ports:
            - name: api-web-port
              containerPort: 80
          env:
            {{- include "tracardi.env" (dict "ctx" . "license" .Values.production.license.existingSecret) | nindent 12 }}
            - name: EXPOSE_GUI_API
              value: "no"
            - name: LOGGING_LEVEL
              value: {{ .Values.production.config.loggingLevel | quote }}
            - name: SAVE_LOGS
              value: {{ .Values.production.config.saveLogs | quote }}
---
apiVersion: v1
kind: Service
metadata:
  name: api-web
spec:
  type: ClusterIP
  selector:
    {{- include "tracardi.selectorLabels" (dict "ctx" . "component" "api-web") | nindent 6 }}
  ports:
    - protocol: TCP
      port: {{ .Values.production.port }}
      targetPort: api-web-port

{{ if .Values.digitalOcean.loadBalancer }}
---
apiVersion: v1
kind: Service
metadata:
  name: do-api-web-lb
  {{ if .Values.digitalOcean.certId }}
  annotations:
    service.beta.kubernetes.io/do-loadbalancer-protocol: "http"
    service.beta.kubernetes.io/do-loadbalancer-tls-ports: "443"
    service.beta.kubernetes.io/do-loadbalancer-certificate-id: {{ .Values.digitalOcean.certId | quote }}
  {{ end }}
spec:
  type: LoadBalancer
  selector:
    matchLabels:
      {{- include "tracardi.selectorLabels" (dict "ctx" . "component" "api-web") | nindent 6 }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.production.port }}
      targetPort: web-api-port
    {{ if .Values.digitalOcean.certId }}
    - name: https
      protocol: TCP
      port: 443
      targetPort: web-api-port
    {{ end }}

{{ end }}
{{ end }}